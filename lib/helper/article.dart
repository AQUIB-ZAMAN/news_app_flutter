class Article {
  String? author, title, desc, content, url, imageUrl;
  DateTime? publishedAt;
  Article({
    this.author,
    this.title,
    this.desc,
    this.content,
    this.url,
    this.imageUrl,
    this.publishedAt,
  });
}
